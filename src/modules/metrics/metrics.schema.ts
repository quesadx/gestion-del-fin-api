// Metrics endpoints are read-only GETs with no request body.
// No Zod schemas required for query params since no pagination is needed.
// Export empty for module consistency with the project convention.
export {};
