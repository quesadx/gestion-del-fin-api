---
phase: 05-fix-all-prisma-schema-issues-based-on-db-review-md-taking-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/modules/professions/professions.service.ts
  - src/jobs/daily-production.job.ts
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "professions_resources_amounts model uses profession_id (not professions_id)"
    - "TypeScript code compiles without errors referencing professions_id"
    - "A Prisma migration exists that renames the FK column"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema with corrected FK naming convention"
      min_lines: 510
    - path: "src/modules/professions/professions.service.ts"
      provides: "Updated profession resource amounts queries"
      contains: "profession_id"
    - path: "src/jobs/daily-production.job.ts"
      provides: "Updated daily production job"
      contains: "profession_id"
    - path: "prisma/migrations/"
      provides: "Migration directory with rename migration"
  key_links:
    - from: "prisma/schema.prisma (professions_resources_amounts.profession_id)"
      to: "src/modules/professions/professions.service.ts"
      via: "Prisma client generated types"
      pattern: "profession_id"
    - from: "prisma/schema.prisma (professions_resources_amounts.profession_id)"
      to: "src/jobs/daily-production.job.ts"
      via: "Prisma client generated types"
      pattern: "profession_id"
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/*/migration.sql"
      via: "prisma migrate dev"
      pattern: "RENAME.*professions_id.*profession_id"
---

<objective>
Rename the inconsistent `professions_id` (plural) FK column to `profession_id` (singular) in the `professions_resources_amounts` model, updating all schema references and TypeScript code to match. Generate a Prisma migration for the rename.

**Purpose:** Fix the last remaining FK naming inconsistency flagged by db-review.md §3.8 — this model uses `professions_id` (plural) while every other FK in the project follows the `[singular]_id` convention (e.g., `camp_id`, `person_id`, `resource_type_id`).

**Output:** A corrected schema.prisma, updated TypeScript service/job files, and a Prisma migration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fix-all-prisma-schema-issues-based-on-db-review-md-taking-in/05-CONTEXT.md
@db-review.md §3.8, §12 (executive summary)
@prisma/schema.prisma (lines 300-315)
@src/modules/professions/professions.service.ts (lines 85-107)
@src/jobs/daily-production.job.ts (lines 35-84)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From prisma/schema.prisma (lines 303-315):
```prisma
model professions {
  // ...
  professions_resources_amounts professions_resources_amounts[]
}

model professions_resources_amounts {
  professions_id   Int
  resource_type_id Int
  amount           Decimal       @db.Decimal(12, 2)
  professions      professions   @relation(fields: [professions_id], references: [id], onDelete: Cascade, onUpdate: Cascade)
  resource_type    resource_types @relation(fields: [resource_type_id], references: [id], onDelete: Restrict, onUpdate: Cascade)

  @@id([professions_id, resource_type_id])
  @@index([resource_type_id])
}
```

From src/modules/professions/professions.service.ts (line 88):
```typescript
professions_id: true,
// ... used in select, orderBy at line 105:
orderBy: [{ professions_id: 'asc' }, { resource_type_id: 'asc' }],
```

From src/jobs/daily-production.job.ts (lines 42, 78):
```typescript
const activeCount = activePeopleByProfession.get(amount.professions_id) ?? 0;
// ...
(amount: ProfessionResourceAmount) => activeProfessionIds.has(amount.professions_id),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename professions_id → profession_id in schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <read_first>
prisma/schema.prisma (lines 300-315)
.planning/phases/05-fix-all-prisma-schema-issues-based-on-db-review-md-taking-in/05-CONTEXT.md (D-02)
  </read_first>
  <action>
In `prisma/schema.prisma`, rename EVERY occurrence of `professions_id` → `profession_id` within the `professions_resources_amounts` model and its reverse relation in the `professions` model:

1. **Line 303** — reverse relation field in `professions` model:
   Change `professions_resources_amounts professions_resources_amounts[]` (no change needed — this is just a relation name, not the FK field)

2. **Line 307** — FK field declaration:
   Change `professions_id   Int` → `profession_id    Int`

3. **Line 310** — @relation fields reference:
   Change `fields: [professions_id]` → `fields: [profession_id]`

4. **Line 313** — Composite @@id:
   Change `@@id([professions_id, resource_type_id])` → `@@id([profession_id, resource_type_id])`

Do NOT rename the model name `professions_resources_amounts` — only the FK column `professions_id` → `profession_id`. Do NOT add `map:` names or modify anything else in this task.
  </action>
  <acceptance_criteria>
- `grep -n "professions_id" prisma/schema.prisma` returns ZERO matches (the old name is gone)
- `grep -n "profession_id" prisma/schema.prisma` returns at least 3 matches in lines 307, 310, 313
- The `professions` model line 303 still references `professions_resources_amounts[]` (unchanged)
- `npx prisma validate` exits 0 (schema is syntactically valid)
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>The schema.prisma `professions_resources_amounts` model uses `profession_id` (singular) instead of `professions_id` (plural). `npx prisma validate` passes.</done>
</task>

<task type="auto">
  <name>task 2: Update TypeScript code references from professions_id to profession_id</name>
  <files>
src/modules/professions/professions.service.ts
src/jobs/daily-production.job.ts
  </files>
  <read_first>
src/modules/professions/professions.service.ts (full file, 107 lines)
src/jobs/daily-production.job.ts (full file, 131 lines)
.planning/phases/05-fix-all-prisma-schema-issues-based-on-db-review-md-taking-in/05-CONTEXT.md (D-03)
  </read_first>
  <action>
Replace all `professions_id` references with `profession_id` in:

**src/modules/professions/professions.service.ts:**
- Line 88: `professions_id: true,` → `profession_id: true,`
- Line 105: `[{ professions_id: 'asc' }, { resource_type_id: 'asc' }]` → `[{ profession_id: 'asc' }, { resource_type_id: 'asc' }]`

**src/jobs/daily-production.job.ts:**
- Line 42: `amount.professions_id` → `amount.profession_id`
- Line 78: `amount.professions_id` → `amount.profession_id`

Use exact string replacement. Do NOT change any other code. The TypeScript types will be regenerated by Prisma client after migration, so the property names will match the new schema field name.
  </action>
  <acceptance_criteria>
- `grep -rn "professions_id" src/modules/professions/professions.service.ts` returns ZERO matches
- `grep -rn "professions_id" src/jobs/daily-production.job.ts` returns ZERO matches
- `grep -c "profession_id" src/modules/professions/professions.service.ts` returns exactly 2
- `grep -c "profession_id" src/jobs/daily-production.job.ts` returns at least 2 (the `.profession_id` references + context references)
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "professions_id" src/modules/professions/professions.service.ts src/jobs/daily-production.job.ts; test $? -eq 1</automated>
  </verify>
  <done>Zero occurrences of `professions_id` remain in TypeScript source files. All references use `profession_id` (singular).</done>
</task>

<task type="auto">
  <name>task 3: Generate Prisma migration for the field rename</name>
  <files>prisma/migrations/ (new migration directory created)</files>
  <read_first>
prisma/schema.prisma (lines 300-315 — verify rename is in place)
prisma/migrations/ (list existing migrations to see naming convention)
  </read_first>
  <action>
Generate a Prisma migration for the `professions_id` → `profession_id` rename:

```bash
npx prisma migrate dev --name rename_professions_id_to_profession_id
```

This command will:
1. Detect the schema change (field rename in `professions_resources_amounts`)
2. Generate a SQL migration file in `prisma/migrations/{timestamp}_rename_professions_id_to_profession_id/`
3. Apply the migration to the development database
4. Regenerate the Prisma client (`node_modules/.prisma/client/`)

After migration, verify the TypeScript build compiles:
```bash
npx tsc --noEmit
```

Per D-04: Use the migration name exactly `rename_professions_id_to_profession_id`.
  </action>
  <acceptance_criteria>
- A new directory exists: `prisma/migrations/*_rename_professions_id_to_profession_id/`
- The migration SQL file contains an ALTER TABLE statement renaming `professions_id` to `profession_id`
- `npx tsc --noEmit` exits 0 (TypeScript compiles without errors with regenerated Prisma client)
- `grep -r "profession_id" prisma/migrations/*_rename_professions_id_to_profession_id/` returns matches
  </acceptance_criteria>
  <verify>
    <automated>ls prisma/migrations/*_rename_professions_id_to_profession_id/migration.sql && npx tsc --noEmit</automated>
  </verify>
  <done>Migration `rename_professions_id_to_profession_id` exists, has been applied to DB, Prisma client is regenerated, and TypeScript compiles cleanly with the new `profession_id` field name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a schema field rename — no new trust boundaries are created or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | prisma/schema.prisma | accept | Field rename only — no functional change. E2E tests in Plan 02 will catch regressions. |
| T-05-02 | Information Disclosure | N/A | accept | No PII, auth, or data access changes in this plan. |
| T-05-03 | Denial of Service | N/A | accept | Index changes (Plan 02) improve query performance, not degrade it. |
</threat_model>

<verification>
1. `npx prisma validate` — schema is valid after rename
2. `grep -rn "professions_id" prisma/schema.prisma src/` — ZERO matches (complete rename)
3. `npx tsc --noEmit` — TypeScript compiles without errors
4. Migration file exists and is applied to DB (`npx prisma migrate status`)
</verification>

<success_criteria>
- [ ] `prisma/schema.prisma` uses `profession_id` (singular) in all 3 locations within `professions_resources_amounts`
- [ ] `src/modules/professions/professions.service.ts` has zero `professions_id` references, 2 `profession_id` references
- [ ] `src/jobs/daily-production.job.ts` has zero `professions_id` references
- [ ] Prisma migration `rename_professions_id_to_profession_id` exists and is applied
- [ ] `npx tsc --noEmit` passes (TypeScript compiles)
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-all-prisma-schema-issues-based-on-db-review-md-taking-in/05-01-SUMMARY.md`
</output>
