# Plan: Update e2e tests for CI compatibility

## Goal
Make e2e tests CI-compatible: update CI workflow to run e2e tests with PostgreSQL, add `npx playwright install` and prisma db push steps.

## must_haves
- CI workflow runs e2e tests after unit tests
- PostgreSQL service provisioned in CI with test database created
- Prisma generate + db push runs before e2e tests
- Playwright browsers installed in CI
- No functional changes to test spec files (they match current API)

## Tasks

### Task 1: Update CI workflow
**Files:** `.github/workflows/ci.yml`
**Action:** Add PostgreSQL service, database creation, prisma generate + db push, playwright install, and e2e test step
**Verify:** CI has PostgreSQL 16 service, creates `gestion_del_fin_test` database, runs e2e tests
**Done:** CI workflow runs `npm run test:e2e` with proper database setup
