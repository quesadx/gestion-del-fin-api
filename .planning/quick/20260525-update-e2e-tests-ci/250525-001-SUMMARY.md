---
status: complete
---

## Summary: Update e2e tests for CI

### Analysis
After thorough review of all 20 e2e test files, 14 route module definitions, and the Prisma schema:
- **Test spec files match current API routes** — no endpoint/path mismatches
- **People cross-camp test** returns 409 (confirmed correct via `people.service.ts:293-294`)
- **All roles get all permissions** in global setup, matching permissionMiddleware requirements
- **Image upload middleware** is a no-op for JSON requests (skips via `req.is('multipart/form-data')` check)
- **DELETE body on expeditions** works with Playwright + express.json()

### Changes
1. **`.github/workflows/ci.yml`** — Split into two jobs:
   - `lint-and-unit`: ESLint, Prettier, spell check, Jest unit tests (cache: npm)
   - `e2e`: PostgreSQL 16 service, `npm ci`, playwright install chromium, prisma generate + db push, run e2e tests
   - Both jobs use `actions/setup-node@v3` with npm caching
   - DATABASE_URL set at job level matching `.env.test` credentials

### Why no test code changes needed
All 14 API modules were verified against the actual route files, service implementations, and Prisma schema. The tests correctly match the current API behavior. The only gap was CI infrastructure, which this task addresses.
